import { ProjectsHeader } from "./ui/projects-header";
import { ProjectsList } from "./ui/projects-list";
import { ProjectsModal } from "./ui/projects-modal";
import { ProjectsRoot } from "./ui/projects-root";
import { ProjectsRow } from "./ui/projects-row";

export const Projects = {
  Header: ProjectsHeader,
  List: ProjectsList,
  Modal: ProjectsModal,
  Root: ProjectsRoot,
  Row: ProjectsRow,
};
